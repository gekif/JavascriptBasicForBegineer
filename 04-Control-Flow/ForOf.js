/**
 * Only work in ecmascript
 */

const colors = ['red', 'green', 'blue'];


for (let color of colors)

    // Print only the value
    console.log(color);

    // Print all array
    console.log(colors);
